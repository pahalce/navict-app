import Image from 'next/image'

const BarVertex = () => {
  return (
    <div className={`flex justify-center`}>
      <Image src={`/bar-vertex.svg`} width={32} height={32} />
    </div>
  )
}

export default BarVertex
